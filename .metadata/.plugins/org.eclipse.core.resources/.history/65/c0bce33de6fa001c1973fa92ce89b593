package com.skillstorm;

import java.sql.Connection;
import java.sql.DriverManager;


/**
 * @author phili
 * will take input from user to query database and return
 * info about their expense reimbursement status
 */
public class Expense {
	public static void main(String[] args) {
		//establish the connection
		//we cant instantiate a connection because its an interface 
		//so we use driver managers static method
		Connection conn;
		
		String url = "";
		
		String username = "";
		
		String password = "";
		
		//were going to need to load these values from a properties file usong classloader
		
		
		DriverManager driverManager = DriverManager.getConnection(null, null, null);
		
		
		//create a sql statement
	}
}
